/**
 * General interfaces for all repositories. Repositories are place to implement fetching objects for underlying data
 * stores (e.g. database, filesystem, memory, etc.). Repositories are only place where code should be aware of how
 * data is stored.
 */
package pl.edu.pg.eti.kask.rpg.repository;
